---
# prepares the host for general access, configures firewall,
# general utility packages and installs docker. The workshop
# user is created and ssh keys are put in place.
- hosts: all
  roles:
    - base-os
# we need to disable the firewall until
# k8s is back in the game :-/
#    - firewall
    - docker
    - tls
