---
- name: create target directories
  file:
    name: /etc/certficates
    state: directory

- name: place tls wildcard cert
  copy:
    src: "{{ inventory_hostname }}.{{ workshop_domain }}.pem"
    dest: "/etc/certficates/{{ inventory_hostname }}.{{ workshop_domain }}.pem"
