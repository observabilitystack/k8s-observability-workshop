---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  labels:
    application: grafana
    component: monitoring
  name: grafana
spec:
  rules:
  - host: grafana.${HOSTNAME}.k8s.o12stack.org
    http:
      paths:
      - backend:
          serviceName: grafana
          servicePort: web
