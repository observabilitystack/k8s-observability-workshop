---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  labels:
    application: grafana
    component: monitoring
  name: grafana
spec:
  rules:
  - host: grafana.${HOSTNAME}.${DOMAIN_NAME}
    http:
      paths:
      - backend:
          serviceName: grafana
          servicePort: web
